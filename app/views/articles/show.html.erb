<h1>This is the show view for Articles</h1>
<div>
  <h2 class = "strong"> <%= @article.title %> </h2>
  <p>Published <%= @article.created_at.strftime('%b %d, %Y') %></p>
  <p>Filed under: <% @article.categories.each do |category| %>
    <%= category.name %>&nbsp;<% end %>
  </p>
  <div class = "lead"><%= @article.content %></div>
</div>